# ADR 0001：采用模块化单体

状态：已接受

## 背景

项目未来包含书库、阅读器、地图、人物、时间线和文博功能。如果各功能独立建模和选型，会形成多个无法协同的平行系统。

## 决策

采用模块化单体。共享唯一领域模型、数据契约、应用端口和发布流程。模块只负责业务能力和界面，不拥有第二套基础架构。

## 后果

- 新功能接入成本可预测；
- 跨模块联动通过稳定 ID 和用例完成；
- 初期部署简单；
- 如果未来确需拆分服务，也必须先从现有端口后方替换适配器，而不是复制领域逻辑。
