[project]
name = "infinite-spacetime-pipeline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["jsonschema[format]>=4.26,<5", "pypdf>=5.0,<7"]

[project.optional-dependencies]
test = ["coverage==7.15.2"]

[project.scripts]
infinite-spacetime-data = "infinite_spacetime_pipeline.cli:main"
