[project]
name = "infinite-spacetime-pipeline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
infinite-spacetime-data = "infinite_spacetime_pipeline.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
